<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application>

        <provider
            android:name="com.therouter.InnerTheRouterTrojan"
            android:authorities="${applicationId}.therouter.TheRouterTrojan"
            android:exported="false" />
    </application>

</manifest>
